# 冻结v4候选最终回归

日期：2026-09-15。用户明确先commit、冻结v4、完成四项最终回归，并行准备进程级关键用例；不无限追加mock测试，生产验收单列。

## 冻结对象

- 提交：`2bc12b363e62923ca6c1db0185e42f9ed5c78bf9`
- 分支：`ghcp-user-pool-mysql`
- 提交说明：`feat(pool): add shared MySQL multi-proxy user pool`
- v4 Docker镜像ID：`sha256:826d609963e0951258621a87334ea2a6e6af91d39280940ae80bf477c3364c3c`
- 已commit，**未push**。后续测试报告和新进程级测试准备不是冻结生产源码变更。
- 67份生产源码及构建输入在云端workspace中一致；镜像内66份未变输入一致。运行时package-lock经构建裁剪后仅11个平台可选包的peer元数据不同，无新增/删除包、版本/resolved/integrity变化；单独留证，不声称运行时lockfile字节相同。
- 冻结提交完整Proxy回归418pass/0fail/25条件skip及workspace/upgrade类型检查再次通过；MySQL条件入口另有既往执行证据。

## 本轮封闭验收集合

| 项目 | 固定范围 | 结果 |
| --- | --- | --- |
| 30分钟持续故障负载 | 新空卷、先smoke、12预热成员、6并发、1800秒、Proxy退出＋MySQL暂停 | **通过：1803秒、5095请求、0意外错误，最终hold0** |
| 真实MySQL重启 | 已开始SSE、停库35秒、双Proxy安全失败、无需重启Proxy恢复、owner/lease/hold检查 | **通过：停止窗口35.149秒，双Proxy503、新owner、hold0** |
| 0→2000自动建池 | 新空卷、真实Worker、mock外部链路、prewarm5/pending5/Login1/50ms，不seedReady | **通过：619.668秒全部Ready，4次跨副本canary成功** |
| 2000caller请求负载 | 另一新空卷、2000合成Ready、25并发、2轮4000推理及管理/目录 | **通过：4022次测量HTTP全成功，0错误，租约与hold检查通过** |

各场景串行占用共享云端环境，无并行镜像构建/其他重负载。不得在测试中途更换生产实现或悄悄提高超时。任何产品阻塞出现都令本候选不能放行，保留失败、另行修复与重新冻结，不把混合版本报告拼成通过。

独立caller队列取消安全审查已在冻结提交上完成有界只读复核：9次工具调用、未发现确认缺陷；不是穷尽审计或额外运行时验证。此前未完成审查仍保留历史。

## 并行准备，不混入当前候选

- 独立Node进程的catalog取消/推理及旧凭据响应竞态关键用例。
- 独立Worker进程的Login已受理后owner退出、原nonce恢复关键用例。

各agent在自己的工作目录，只新增test harness/说明，不改生产代码、不操作共享Azure环境、不额外commit。两个准备任务均完成：

- 路由组8文件：独立子进程catalog部分取消＋推理、跨进程旧200后旧401的ABA关键用例；语法/严格类型检查及2个离线安全门禁通过。反向返回顺序、全部消费者取消和count_tokens未在此准备覆盖。
- worker组7文件：Login已受理且checkpoint受控暂停时SIGKILL旧进程，等待实际30秒任期到期并由新进程恢复原nonce；另有超龄任务/迟到回调的终态保护用例。类型/语法和2个离线门禁通过，接受性入口明确skip。

**这些仅准备完成，未执行实际MySQL/独立进程验收，也未合入冻结候选。** 文件和SHA清单保留在各独立工作目录及私有接续记录；不将准备或离线门禁当作进程级场景通过。

## 1. 冻结候选持续故障测试

实际1803秒，5095请求：4662完整成功、259主动取消、174预期请求失败；expectedErrors175另含1管理采样，不与HTTP互斥结果简单相加。68组429/冷却检查、274次延迟SSE、1次原成员401恢复（约1.528秒）和非活跃caller自然到期通过。活跃caller自然epoch变化0，401相关变化1。

Proxy停止/恢复控制窗口39.415秒、survivor成功96次；MySQL主动暂停12秒，总控制/恢复窗口14.471秒。故障恢复宽限外0意外错误，最终12成员/6租约/hold0/cataloghold0。5095个客户端marker中4994唯一marker达上游，101未达上游；没有失败请求自动重放，背景warmup1，SCIM12/Login任务13/成功回调13。

完整报告`v4-final-soak-report.json`，SHA-256：`0697077ca17d26b854fd5e0d69bcd4735f19990e85ba895b5d999b20266d6b15`。这次结果属于冻结提交2bc12b3和v4镜像，不再借用v3历史结果。

## 2. 冻结候选真实MySQL重启

MySQL停止至start完成35.149秒，完整故障/恢复核对40.850秒；两副本故障期503，恢复无需Proxy重启、新有效owner任期。一个已激活基准lease的member/ID/expiry/last-success和共享settings在失败流之后保持，随后正常请求成功。

SSE收到20块893字节后约5.016秒以socket中断结束，未出现成功终止、EOF或客户端abort/fallback；上游4个marker唯一，最终hold/cataloghold0。报告SHA-256：`873a9861921b01b94cd202ff9e224566874858669f711f2764792b7cf5d52755`。

## 3. 冻结候选0→2000自动建池

全新`v4-final-provision`数据集，由真实Worker完成外部mock链路，建池619.668秒、总验收627.495秒，平均3.23Ready/秒；最终2000Ready及完整计数、分页、两caller四次跨副本canary通过。测试发生器不直接写入Ready；最终测试租约释放、hold0、Worker暂停。

报告SHA-256：`864f3e50c8e5a01137d35a69de6be136b01e56f540984f65c4babe40ec0ceca7`。这是一秒以内mock服务延迟条件下的正确性/规模回归，不是真实浏览器或GitHub生产速度承诺。

## 4. 冻结候选2000caller请求负载

独立新空卷，发生器预置2000合成Ready仅用于请求负载；不与前项真实Worker建池混用。25并发、2轮共4000推理＋22管理/目录请求，**4022成功、0错误**。

- 第一轮：11.143秒，179.48成功请求/秒。
- 第二轮：8.190秒，244.19成功请求/秒。
- SQL观察39次，最高观察到25个hold，观察断言无失败。
- 全2000caller活跃排他lease、跨副本member/lease保持、各协议JSON/SSE、精确page20、统计归属、目录不续租、上游marker唯一及最终hold0全部通过。

固定mock延迟20ms，数字仅为该Azure测试资源和小合成响应下的结果，不承诺真实模型吞吐，不由此推断同比性能提升原因。报告SHA-256：`1686e17320562695b106f6e1dac227f65dd4bfefd75d01acc95f5dd3569aa322`。

## 执行记录

- 私有后台包装脚本首次以CRLF写入Linux，shell第一行退出，smoke/业务流量尚未启动。该次只算启动失败，原日志保留；包装脚本转LF，以独立任务重新启动，冻结代码/镜像未变。
- 源码校验脚本曾因跨层字符串转义失败，修正后得到66份镜像输入一致及lockfile元数据差异的明确结果；这些不是产品测试失败，也不以Azure命令返回0代替实际断言。

## 候选结论：本次冻结v4隔离回归通过

四项闭集验收全部通过，测试期间生产源码、已提交夹具和Proxy镜像未更换。提交`2bc12b3`对应的v4可作为**已通过本次隔离最终回归的候选版本**；这不是生产部署授权、最大容量或全系统高可用认证。本轮到此结束，不无限追加mock测试；未执行的进程级关键用例作为下一阶段准备保留。

### 独立生产上线门槛

1. Rancher实际Service/EndpointSlice和跨节点部署核对，授权测试环境的Pod/节点故障与滚动升级。
2. MySQL主库HA写端点、主切换、TLS、备份恢复和灾备；本轮同一MySQL容器重启不等于HA提升。
3. 客户一致性备份迁移、回退与人工核对处置演练。
4. 单独授权的小规模真实Login/GitHub验证和代表性业务容量目标；不擅自加付费席位。
5. SSO/Login/Console单实例影响及恢复目标、生产监控和回调故障处理。

本地提交已完成，未push。四份完整结果已下载并验证SHA；结束后池健康，paused1/target0，2000个合成成员仍有2000个活跃测试租约、hold/cataloghold0，未删除数据或重置租约。所有本机preview和测试任务结束，AzureVM保持运行。并行准备的独立进程测试未合入冻结提交、未执行云端验收；本报告属于提交后的验证记录。
